from django.apps import AppConfig


class TickerchartappConfig(AppConfig):
    default_auto_field = 'django.db.models.BigAutoField'
    name = 'tickerchartapp'

    # def ready(self):
    #     print("started")
